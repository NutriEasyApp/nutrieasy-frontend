import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    background: {
        flex: 1,
        alignItems: 'center',
        justifyContent: 'center',
        backgroundColor: '#fff'
    },   
    container: {
        alignItems: 'center',
        justifyContent: 'center',
        width: '100%',
        paddingBottom: 0,
        marginTop: 0,
    }
});